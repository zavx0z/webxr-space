{
  "name": "webxr-space",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/three": "^0.180.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "upd": "bun update --latest && bun i",
    "build": "mkdir -p dist && cp -r . dist/ && rm -rf dist/.git dist/.github dist/node_modules dist/ssl dist/tsconfig.json dist/worker-virtual.config.js dist/worker-virtual.t.ts && cd dist && bun install --production",
    "dev": "bun --hot index.ts",
    "start": "bun index.ts"
  },
  "dependencies": {
    "@metafor/atom": "^0.4.5",
    "@metafor/inspect": "^0.4.0",
    "@metafor/meta": "^0.4.1"
  }
}