import {test} from "bun:test"

test.todo("[DOM-001] Каждый Element принадлежит ровно одному Document", () => {})
test.todo("[DOM-002] Document сохраняет identity Element до его явного удаления", () => {})
test.todo("[DOM-003] Same-Document reparent сохраняет identity, listeners, focus и состояние Element", () => {})
test.todo("[DOM-004] Атрибуты, события, focus и состояние полей принадлежат semantic Element", () => {})
