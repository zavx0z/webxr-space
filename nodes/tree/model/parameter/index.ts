/**
Значения отдельных параметров графа.

Store сохраняет собственную identity, выпускает immutable снимки и уведомляет
подписчиков только после изменения значения; повтор того же значения остаётся no-op.

@packageDocumentation
*/

export * from "./src/index.ts"
