# Параметры

Пакет `@nodes/parameters`. Описания категорий и компонентов находятся в их `index.ts`.
Реализации — в `src` компонентов, проверки — в `tests`, общая проекция Parameter Store — в `shared`.
