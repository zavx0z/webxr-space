# HTTPS Setup для WebXR

## 🚀 Быстрый старт

### 1. Запуск HTTPS сервера

```bash
bun run dev
```

### 2. Доступ к приложению

- **Локально**: https://localhost:3000
- **По сети**: https://172.20.10.2:3000

## 📱 Настройка для VR гарнитуры

### Meta Quest / Oculus

1. Убедитесь, что гарнитура подключена к той же Wi-Fi сети
2. Откройте браузер в гарнитуре
3. Перейдите по адресу: `https://[YOUR_IP]:3000`
4. При первом подключении:
   - Нажмите "Дополнительно"
   - Выберите "Перейти на сайт (небезопасно)"
   - Это необходимо из-за самоподписанного сертификата

### Другие VR гарнитуры

- Используйте встроенный браузер
- Адрес: `https://[YOUR_IP]:3000`

## 🔧 SSH туннель

### Для удаленного доступа

```bash
# Настройка SSH туннеля
./ssh-tunnel.sh your-server.com 22

# Или вручную
ssh -L 3000:localhost:3000 -N your-server.com
```

### Использование туннеля

После создания туннеля используйте: `https://localhost:3000`

## 🛠️ Разработка

### HMR (Hot Module Replacement)

- Автоматически включен в режиме разработки
- Изменения в коде применяются без перезагрузки
- WebSocket соединение для live reload

### SSL сертификаты

- Сертификаты находятся в папке `ssl/`
- Самоподписанные для разработки
- Для продакшена замените на валидные сертификаты

## 🔍 Отладка

### Проверка подключения

```bash
# Проверка портов
netstat -an | grep 3000

# Проверка SSL
openssl s_client -connect localhost:3000
```

### Логи сервера

Сервер выводит:

- IP адреса локальной сети
- Статус HMR
- WebSocket соединения

## 📋 Требования

- Bun runtime
- VR гарнитура с браузером
- Локальная сеть (Wi-Fi)
- SSL сертификаты (автоматически созданы)

## ⚠️ Безопасность

- Используйте только в доверенной сети
- Самоподписанные сертификаты не подходят для продакшена
- Для продакшена получите валидные SSL сертификаты
