# @nodes/ui destination

Reserved for one accepted component path: NodeTree, NodeEditor, Frame, Node,
concrete Parameters, Socket and Link. That path now exists at canonical Node
commit `9ddded8` with mandatory exact owner Layout, copy-on-write append, stable
Parameter Stores, verified technical R5 performance and exact Blender 5.2
reference evidence. UI `1ddae57` restores the historical SVG select indicator
while retaining native select interaction ownership. Node `ed7b6a9` corrects
the rejected input-Socket contour offset. A new explicit equal-scale visual
verdict and R6 still prevent import.
