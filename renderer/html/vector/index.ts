/**
 * Векторные контуры: разбор, нормализация и внутренняя область.
 *
 * Модуль владеет ограниченной абсолютной грамматикой одного контура M/L/Q/C.
 * Нормализованные кубические участки и sampled рёбра являются общими входами
 * paint и hit testing; заливка неявно замыкает контур по nonzero или evenodd.
 *
 * DOM хранит исходное d, Renderer удерживает geometry по версии исходника,
 * WebGPU владеет tessellation и графическими ресурсами. Модуль не создаёт
 * DOM-элементы, GPU-ресурсы или отдельный кэш. Это внутренний вход HTML Renderer;
 * публичный набор package exports остаётся прежним.
 */
export {parseRenderPath, pointInPathFill} from "./src/path.ts"
