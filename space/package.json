{
  "name": "@zavx0z/space",
  "version": "0.0.0",
  "description": "Space, ViewPoint, Mesh, Geometry, Material, Display и HUD",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "test": "bun test tests",
    "check": "bun run typecheck && bun run test"
  }
}
